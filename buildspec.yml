version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm install
  build:
    commands:
      - zip -r lambda.zip index.js package.json node_modules public
      - aws s3 cp lambda.zip s3://$LAMBDA_ARTIFACT_BUCKET/$LAMBDA_ARTIFACT_KEY
artifacts:
  files: []
